const addon = require('./build/Release/addon');

console.log(addon.hello()); // This should print "Hello from C++!"

// Additional code to start your web server or application logic can be added here.